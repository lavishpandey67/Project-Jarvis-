{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "lib/*",
    "artifacts/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npx pnpm --filter @workspace/personal-ai-workforce run build && npx pnpm --filter @workspace/api-server run dev",
    "start": "npx pnpm --filter @workspace/api-server run start",
    "build": "npx pnpm run typecheck && npx pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npx pnpm run typecheck:libs && npx pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
