import { Bot, CheckCircle2, CircleDashed, Cpu, Sparkles } from 'lucide-react';
import { useListAgents } from '@workspace/api-client-react';
import { EmptyState, ErrorState, LoadingState, PageIntro } from '@/components/workspace-shell';

export default function AgentsPage() {
  const agents = useListAgents();
  return <div><PageIntro eyebrow="Specialized workforce" title="Agents" description="The Companion keeps the roster small on purpose. Each specialist has a clear lane and a reason to exist." /><div className="mb-6 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-4"><p className="font-mono-app text-[10px] uppercase tracking-[.14em] text-muted-foreground">Registered</p><p className="mt-3 font-display text-3xl font-semibold text-primary">{agents.data?.length ?? '—'}</p></div><div className="rounded-2xl border border-border bg-card p-4"><p className="font-mono-app text-[10px] uppercase tracking-[.14em] text-muted-foreground">Active now</p><p className="mt-3 font-display text-3xl font-semibold text-[#4d8b67]">{agents.data?.filter((agent) => agent.status === 'active').length ?? '—'}</p></div><div className="rounded-2xl border border-border bg-card p-4"><p className="font-mono-app text-[10px] uppercase tracking-[.14em] text-muted-foreground">Selection model</p><p className="mt-3 text-sm font-semibold text-primary">One clear owner</p></div></div>{agents.isLoading ? <LoadingState rows={3} /> : agents.isError ? <ErrorState onRetry={() => agents.refetch()} /> : (agents.data ?? []).length === 0 ? <EmptyState icon={Bot} title="No specialists registered." description="When agents are available, they will appear here with their lane and current status." /> : <div className="grid gap-4 md:grid-cols-2">{agents.data?.map((agent, index) => <article key={agent.id} className="relative overflow-hidden rounded-2xl border border-border bg-card p-6" data-testid={`card-agent-${agent.id}`}><div className={`absolute right-0 top-0 h-24 w-24 rounded-bl-[60px] ${index % 2 ? 'bg-accent/25' : 'bg-primary/5'}`} /><div className="relative flex items-start justify-between"><span className="grid size-11 place-items-center rounded-2xl bg-primary text-primary-foreground"><Cpu size={20} /></span><span className={`flex items-center gap-1.5 rounded-full px-2.5 py-1 font-mono-app text-[9px] uppercase tracking-[.08em] ${agent.status === 'active' ? 'bg-[#e8f3e9] text-[#4d8b67]' : 'bg-muted text-muted-foreground'}`}>{agent.status === 'active' ? <CheckCircle2 size={12} /> : <CircleDashed size={12} />}{agent.status}</span></div><h2 className="mt-6 font-display text-[25px] font-semibold tracking-[-.04em]">{agent.name}</h2><p className="mt-1 font-mono-app text-[10px] uppercase tracking-[.14em] text-[#a76f43]">{agent.role}</p><p className="mt-5 text-sm leading-6 text-muted-foreground">{agent.description}</p><div className="mt-6 flex items-center gap-2 border-t border-border pt-4 text-xs text-muted-foreground"><Sparkles size={13} className="text-accent-foreground" /> Selected by the Companion when this lane is right</div></article>)}</div>}</div>;
}